import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
    base: '/wp_custom_update_manager/',  // Replace with your repo name
    outDir: 'dist',
});
